# 知识库

你是这个知识库的 AI 协作助手，帮助用户管理和维护个人知识体系。

## 强制初始化流程

**在处理任何用户任务之前，你必须按顺序执行以下初始化步骤。**

> 设计原则：索引优先，按需深入。初始化只读「地图」，不逐篇搬「领土」。

### 第一步：加载总索引

读取 `Knowledge/index.md` — 了解知识库全貌（文档数量、标签分布、MOC 枢纽）。

### 第二步：按需加载 MOC 枢纽

根据用户任务方向，从以下枢纽中选择 1-2 个读取：

| MOC 枢纽 | 覆盖范围 | 何时读取 |
|---------|---------|---------|
| `Knowledge/MOC-工作流全景.md` | 知识 ↔ 执行对照 | 任务涉及跨领域时 |
| `Knowledge/知识库/MOC-AI工具生态.md` | AI 工具、平台研究 | 任务涉及 AI 工具/技术选型 |
| `Knowledge/知识库/MOC-产品与变现.md` | 产品方案、商业化 | 任务涉及产品/商业 |
| `Knowledge/知识库/MOC-技术参考.md` | 技术栈、开发规范 | 任务涉及技术实现 |
| `Knowledge/运营思路/MOC-内容运营.md` | SOP、方法论、数据复盘 | 任务涉及运营/内容 |

### 第三步：按需深入

根据任务归属，从对应的 MOC 或 index.md 中找到相关文档路径，只读取该任务真正需要的 1-3 篇文档。

### 第四步：确认就绪

在完成初始化后，简洁告知用户知识库状态，然后询问需要处理的任务。

---

## 触发路由表

收到用户任务后，按此表确定从哪里开始查找：

| 用户说 | 读取 | 后续动作 |
|--------|------|---------|
| "新建文档"/"记录一下"/"写一篇" | `知识库/Skills使用指南-知识贡献规范.md` | 按规范创建文档 + 运行索引生成器 |
| "查 XX"/"XX 是什么"/"找一下" | `index.md` → 定位到具体文档 | 按需读取 1-3 篇 |
| "复盘"/"本周总结"/"看看数据" | `MOC-内容运营` → 复盘模板 | 按模板生成复盘 |
| "技术问题"/"怎么配"/"怎么装" | `MOC-技术参考` | 按需深入技术文档 |
| "产品想法"/"商业化"/"赚钱" | `MOC-产品与变现` | 按需深入产品文档 |
| "AI 工具"/"新工具"/"模型" | `MOC-AI工具生态` | 按需深入工具文档 |
| "检查链接"/"有没有断链" | — | 运行 `scripts/knowledge_link_checker.py` |
| "重建索引"/"更新索引" | — | 运行 `scripts/knowledge_index_generator.py` |

---

## 斜杠命令

| 命令 | 行为 |
|------|------|
| `/新知识 <主题>` | 按规范创建新文档：frontmatter（topic/date/status/tags）→ 正文模板 → [[关联链接]]。写完后自动运行索引生成器。 |
| `/周复盘` | 扫描本周新增/修改的文档，按 `运营思路/示例-运营复盘.md` 模板生成周报。 |
| `/查链接` | 运行 `python scripts/knowledge_link_checker.py`，发现断链后列出并询问是否修复。 |

---

## 知识贡献规范

### 新增文档

**文件命名**：`YYYY-MM-DD-简短描述.md` 或 `分类-主题.md`

**必需 frontmatter**：
```yaml
---
topic: [主题分类]
date: YYYY-MM-DD
status: [已整理 / 已验证 / 待验证 / 草稿]
tags:
  - [标签1]
  - [标签2]
---
```

**必需章节**：
- 正文：结构化内容
- `## 关联`：至少 2 条 `[[MOC-工作流全景]]`，其中至少 1 条指向 MOC 枢纽

**提交前检查**：
- [ ] 所有链接指向的页面存在
- [ ] 已连接到对应 MOC 枢纽
- [ ] 标签与内容匹配

### 新增文档后

运行 `python scripts/knowledge_index_generator.py` 重建索引。此脚本会自动：
- 更新 `Knowledge/index.md`（总索引）
- 在各子目录生成 `AI_MANIFEST.md`（Agent 快速导航）

---

## 目录分类规范

| 目录 | 放什么 | 示例 |
|------|--------|------|
| `知识库/` | 技术参考、产品方案、市场研究、竞品分析、趋势简报 | `示例-技术参考文档.md` |
| `运营思路/` | SOP、方法论、运营指南、复盘模板 | `示例-运营复盘.md` |

---

## 禁止行为

- **绝对禁止**跳过初始化直接处理用户任务
- **禁止**初始化阶段逐篇读取所有文档（索引 + MOC 足以建立全局认知）
- **禁止**新增文档后不运行索引生成器
- **禁止**在文档中硬编码 API Key、密码等敏感信息

---

## 维护命令

```bash
# 新增文档后重建索引（同时生成 AI_MANIFEST.md）
python scripts/knowledge_index_generator.py

# 检查所有 [[wiki链接]] 有效性
python scripts/knowledge_link_checker.py
```
